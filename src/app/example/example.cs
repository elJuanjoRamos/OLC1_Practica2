public class Example
{
    static void Main(string[] args)
    {
       //SWITCH
        switch (coll)
        {
            case "arr":
               break;
            case "eInt":
            case 1:
               break;
            case ie:
               break;
            case null:
               break;
            default:
               break;
        }

         ///SECCION DE IF
        if(hola>1) {

        } else

        if(hola>=1) {
           
        } else

        if(hola<=1) {
           
        } else

        if(hola<1) {
           
        } else

        if(hola==1) {
           
        } else
 /*
         DO WHILE ANIDADO
         */
        if(hola!=1) {
           
        } else {

        }

      //SECCION DO WHILE
        do {

        } while(args>3);

        do {
 /*
         DO WHILE ANIDADO
         */
        } while(args<3);

        do {

        } while(args>=3);

        do {
 /*
         DO WHILE ANIDADO
         */
        } while(args<=3);

        do {

        } while(args==3);

        do {
 /*
         DO WHILE ANIDADO
         */
        } while(args!=3);
        do {
 /*
         DO WHILE ANIDADO
         */
        } while(args==null);
         /*
         DO WHILE ANIDADO
         */
        do {
            do {
            } while(args==false);
        } while(args==true);
    }

}